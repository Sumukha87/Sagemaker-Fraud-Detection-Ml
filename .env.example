# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# LocalStack auth token (free account at https://app.localstack.cloud)
# Only needed for docker-compose.yml (LocalStack option).
# Not needed for docker-compose.floci.yml (Floci option).
LOCALSTACK_AUTH_TOKEN=Test
